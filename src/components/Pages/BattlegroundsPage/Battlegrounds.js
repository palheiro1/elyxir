import { Box, Center } from '@chakra-ui/react';
import { Maps } from './Maps';

const Battlegrounds = () => {
    return (
        <Box>
            <Center>
                <Maps />
            </Center>
        </Box>
    );
};

export default Battlegrounds;
